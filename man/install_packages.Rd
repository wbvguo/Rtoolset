% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{install_packages}
\alias{install_packages}
\title{Install packages from CRAN, Bioconductor, or GitHub}
\usage{
install_packages(
  pkgs,
  repo = c("cran", "bioc", "github"),
  dependencies = TRUE,
  update = FALSE,
  force = FALSE
)
}
\arguments{
\item{pkgs}{Character vector of package identifiers.
\itemize{
\item For \code{repo = "cran"} or \code{"bioc"}: package names, e.g. \code{"dplyr"}.
\item For \code{repo = "github"}: \code{"owner/repo"} strings, e.g. \code{"tidyverse/ggplot2"}.
}}

\item{repo}{One of \code{"cran"}, \code{"bioc"}, or \code{"github"}. Default \code{"cran"}.}

\item{dependencies}{Logical; forwarded to \code{utils::install.packages()} and
\code{remotes::install_github()}. Default \code{TRUE}.}

\item{update}{Logical; only used when \code{repo = "bioc"}. If \code{TRUE}, allow
\code{BiocManager::install()} to update other Bioconductor packages. Default \code{FALSE}
to keep installs reproducible.}

\item{force}{Logical; reinstall even if the package appears installed.
(For GitHub, the installed check uses the package name part after \code{/}.)}
}
\value{
Character vector of packages that are still not installed afterward.
}
\description{
Installs each entry in \code{pkgs} from the selected \code{repo}. Skips packages that
are already installed (unless \code{force = TRUE}). For GitHub, pass
\code{"owner/repo"} strings (e.g., \code{"rstudio/gt"}).
}
\examples{
\dontrun{
  install_packages(c("dplyr","ggplot2"))                    # CRAN
  install_packages("SummarizedExperiment", repo = "bioc")   # Bioconductor
  install_packages("rstudio/gt", repo = "github")           # GitHub
}
}
