% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw-heart.R
\name{draw_heart_step}
\alias{draw_heart_step}
\title{Convert heart image to LEGO mosaic and extract build steps}
\usage{
draw_heart_step(
  image_path = NULL,
  heart_image = NULL,
  num_steps = 6,
  width = 50,
  height = 50,
  show = FALSE
)
}
\arguments{
\item{image_path}{Character scalar. Path to the PNG image file to convert.
If NULL and \code{heart_image} is also NULL, a simple heart shape will be generated.}

\item{heart_image}{Optional. Can be a ggplot object (output from \code{draw_heart()})
or a PNG image array. If provided, \code{image_path} is ignored.}

\item{num_steps}{Integer. Number of build steps for the instructions (default: 6).}

\item{width, height}{Integer. Dimensions for the LEGO mosaic (default: 50x50).}

\item{show}{Logical. If TRUE, displays the final mosaic plot. Defaults to \code{FALSE}
(display is currently disabled due to ggplot2/brickr compatibility issues).}
}
\value{
A list with elements:
\describe{
\item{mosaic}{The \code{brickr} mosaic object.}
\item{build}{The build instructions object.}
\item{steps}{List of ggplot objects for each build step.}
}
}
\description{
Converts a heart image to a LEGO mosaic using \pkg{brickr} and extracts
the build steps/panels. The output can be used with \code{draw_heart_gif()} to
create an animated GIF.
}
\examples{
\dontrun{
# Using an existing image file
heart_steps <- draw_heart_step(image_path = "heart.png", num_steps = 6)

# Using a ggplot object from draw_heart()
heart_plot <- draw_heart()
heart_steps <- draw_heart_step(heart_image = heart_plot, num_steps = 6)

# Display the mosaic
print(brickr::build_mosaic(heart_steps$mosaic))
}

}
