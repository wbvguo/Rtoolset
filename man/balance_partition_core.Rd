% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition.R
\name{balance_partition_core}
\alias{balance_partition_core}
\title{Core optimizer: assign samples to groups to balance mean + SD (Gini loss)}
\usage{
balance_partition_core(
  score,
  group_sizes,
  lambda = 1,
  B = 50000,
  method = c("blocked_permute", "random_assign"),
  seed = NULL
)
}
\arguments{
\item{score}{Numeric vector (length n). The scalar to balance.}

\item{group_sizes}{Integer vector of target group sizes. Must sum to \code{length(score)}.}

\item{lambda}{Numeric >= 0. Weight on SD-balance term. Default is \code{1}.}

\item{B}{Integer >= 1. Number of candidate partitions to evaluate.}

\item{method}{Character. One of \code{"blocked_permute"} or \code{"random_assign"}.}

\item{seed}{Optional integer seed.}
}
\value{
A list with:
\itemize{
\item \code{groups}: integer vector (length n) of group labels 1..K
\item \code{loss}: best achieved loss
\item \code{group_sizes}: integer vector of target sizes used
}
}
\description{
Internal/core function that performs the random search and returns the best
grouping. No plotting and no file writing.

The objective minimized is:
\deqn{loss = Gini(group\_means) + lambda * Gini(group\_sds)}
}
