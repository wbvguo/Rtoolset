% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition.R
\name{balanced_partition}
\alias{balanced_partition}
\title{Balanced partition of samples into groups using one numeric column (mean + SD)}
\usage{
balanced_partition(
  data,
  score_col,
  K = NULL,
  group_sizes = NULL,
  id_col = NULL,
  lambda = 1,
  B = 50000,
  method = c("blocked_permute", "random_assign"),
  allow_unequal = TRUE,
  seed = NULL,
  output_dir = NULL,
  file_prefix = "balanced_partition",
  output_csv = TRUE,
  output_plot = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame} with samples in rows.}

\item{score_col}{Name of the numeric column to balance.}

\item{K}{Integer. Number of groups. Ignored if \code{group_sizes} is provided.}

\item{group_sizes}{Optional integer vector of target group sizes; must sum to \code{nrow(data)}.}

\item{id_col}{Optional sample ID column name. If \code{NULL}, uses row index.}

\item{lambda}{Numeric >= 0. Weight on SD-balance term. Default \code{1}.}

\item{B}{Integer >= 1. Number of random tries. Default \code{50000}.}

\item{method}{\code{"blocked_permute"} (default) or \code{"random_assign"}.}

\item{allow_unequal}{Logical. If TRUE and \code{n} is not divisible by \code{K},
group sizes differ by at most 1. Ignored if \code{group_sizes} is provided.}

\item{seed}{Optional integer seed for reproducibility.}

\item{output_dir}{Optional directory path. If provided, outputs are written here.}

\item{file_prefix}{Character. Prefix for output filenames.}

\item{output_csv}{Logical. If TRUE and \code{output_dir} is provided, write CSV
of original data plus appended \code{.__group} and \code{.__score}.}

\item{output_plot}{Logical. If TRUE, generate a ggplot object and (if
\code{output_dir} is provided) write a PNG.}
}
\value{
A list with:
\itemize{
\item \code{assignment}: data.frame with \code{sample_id}, \code{group}, \code{score}.
\item \code{group_stats}: data.frame with per-group \code{n}, \code{mean}, \code{sd}.
\item \code{loss}: best achieved loss.
\item \code{group_sizes}: integer vector of target group sizes used.
\item \code{plot}: ggplot object if \code{output_plot = TRUE}, else NULL.
\item \code{files}: named list of written file paths if any were written, else NULL.
\item \code{params}: list of parameters used.
}
}
\description{
User-facing wrapper around \code{\link{balance_partition_core}}.
Computes the best grouping using \code{score_col}, and optionally generates
a visualization and/or writes outputs to files.
}
