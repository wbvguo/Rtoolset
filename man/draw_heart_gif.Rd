% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_heart.R
\name{draw_heart_gif}
\alias{draw_heart_gif}
\title{Create a LEGO-style animated heart GIF}
\usage{
draw_heart_gif(
  heart_steps = NULL,
  image_path = NULL,
  heart_image = NULL,
  num_steps = 6,
  out_gif = "myheart.gif",
  width = 50,
  height = 50,
  interval = 0.5,
  show = interactive()
)
}
\arguments{
\item{heart_steps}{Optional list. Output from \code{draw_heart_step()} containing
the build steps. If provided, \code{image_path}, \code{heart_image}, \code{num_steps},
\code{width}, and \code{height} are ignored.}

\item{image_path}{Character scalar. Path to the PNG image file to convert.
If NULL and \code{heart_image} is also NULL, a simple heart shape will be generated.
Ignored if \code{heart_steps} is provided.}

\item{heart_image}{Optional. Can be a ggplot object (output from \code{draw_heart()})
or a PNG image array. If provided, \code{image_path} is ignored.
Ignored if \code{heart_steps} is provided.}

\item{num_steps}{Integer. Number of build steps in the animation (default: 6).
Ignored if \code{heart_steps} is provided.}

\item{out_gif}{Character path. Output path for the GIF file.
Default: "myheart.gif" in the current working directory.}

\item{width, height}{Integer. Dimensions for the LEGO mosaic (default: 50x50).
Ignored if \code{heart_steps} is provided.}

\item{interval}{Numeric. Delay between frames in seconds (default: 0.5).
Larger values create slower animations. Ignored if \code{heart_steps} is provided.}

\item{show}{Logical. If TRUE, displays the GIF after saving. Defaults to \code{interactive()}.}
}
\value{
Invisibly returns the \code{heart_steps} list (same structure as \code{draw_heart_step()}).
}
\description{
Creates an animated GIF showing the step-by-step build process of a LEGO heart
mosaic from the build steps. The steps can be generated using \code{draw_heart_step()}.
}
\examples{
\dontrun{
# Create steps and GIF in one step
draw_heart_gif(image_path = "heart.png", out_gif = "heart_animation.gif")

# Or create steps first, then GIF
heart_steps <- draw_heart_step(image_path = "heart.png", num_steps = 6)
draw_heart_gif(heart_steps = heart_steps, out_gif = "heart_animation.gif")

# Control animation speed (slower with larger interval)
draw_heart_gif(heart_steps = heart_steps, out_gif = "slow_heart.gif", interval = 1.0)

# Display the GIF after saving
draw_heart_gif(heart_steps = heart_steps, out_gif = "myheart.gif", show = TRUE)

# Full workflow
heart_plot <- draw_heart(out_png = "myheart.png")
heart_steps <- draw_heart_step(heart_image = heart_plot, num_steps = 6)
draw_heart_gif(heart_steps = heart_steps, out_gif = "myheart.gif", interval = 0.5, show = TRUE)
}

}
