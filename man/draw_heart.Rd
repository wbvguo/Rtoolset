% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw-heart.R
\name{draw_heart}
\alias{draw_heart}
\title{Draw a 2D heart shape image}
\usage{
draw_heart(
  out_png = NULL,
  fill = "red",
  color = "firebrick",
  alpha = 0.9,
  show = interactive(),
  width = 4,
  height = 4
)
}
\arguments{
\item{out_png}{Character scalar. Path where the heart image will be saved.
If NULL, the image is not saved but the ggplot object is returned.}

\item{fill}{Character. Fill color for the heart (default: "red").}

\item{color}{Character. Border color for the heart (default: "firebrick").}

\item{alpha}{Numeric. Transparency level (0-1, default: 0.9).}

\item{show}{Logical. If TRUE, displays the heart plot. Defaults to \code{interactive()}.}

\item{width, height}{Numeric. Width and height of the saved image in inches
(default: 4). Only used when \code{out_png} is provided.}
}
\value{
A \code{ggplot} object. If \code{out_png} is provided, the image is also saved to disk.
}
\description{
Creates a 2D heart shape image using ggplot2. The heart is generated using
parametric equations and can be saved to a PNG file. The saved image can
then be used with \code{draw_heart_step()} to create a LEGO mosaic.
}
\examples{
\dontrun{
# Generate and display a heart shape
heart <- draw_heart()
print(heart)

# Generate and save to file
heart <- draw_heart(out_png = "myheart.png")

# Customize colors
heart <- draw_heart(out_png = "pink_heart.png", fill = "pink", color = "hotpink")
}

}
