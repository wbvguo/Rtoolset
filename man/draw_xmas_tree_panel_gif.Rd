% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_xmas.R
\name{draw_xmas_tree_panel_gif}
\alias{draw_xmas_tree_panel_gif}
\title{Create a panel GIF from multiple style GIFs}
\usage{
draw_xmas_tree_panel_gif(
  styles = 1:9,
  out_gif = "xmas_panel.gif",
  text_fun = function(i) paste0("Style ", i),
  ncol = 3,
  n_frames = 90,
  fps = 50,
  width = 300,
  height = 300,
  text_size = 11,
  anno_size = 3,
  loop = TRUE,
  tmp_dir = NULL,
  show = interactive()
)
}
\arguments{
\item{styles}{Vector of style indices or names (default 1:9). Can be integer indices (1-10) or
character style names (e.g., \code{c("classic christmas", "warm cozy", "vibrant primary")}).
See \code{draw_xmas_tree_gif} for available style names.
The length of \code{styles} must be divisible by \code{ncol}.}

\item{out_gif}{Output GIF file path.}

\item{text_fun}{Function taking style (integer or character) and returning label text for that panel.}

\item{ncol}{Number of columns (default 3). Number of rows is calculated as \code{length(styles) / ncol}.}

\item{n_frames}{Frames per individual GIF (must match across).}

\item{fps}{Frames per second (must match across).}

\item{width, height}{Pixel size per tile (each individual GIF).}

\item{text_size}{Numeric text size for the main title text (default: 12).}

\item{anno_size}{Numeric text size for the annotation text (default: 4).}

\item{loop}{Logical; loop the final GIF.}

\item{tmp_dir}{Where to write temporary per-style GIFs.}

\item{show}{Logical; show final GIF in interactive sessions.}
}
\value{
Invisibly returns the \code{magick-image} animation object.
}
\description{
Create a panel GIF from multiple style GIFs
}
